# 無可可巧克力｜華文文獻閱讀與寫作

互動式教學簡報（單一 HTML 檔），主題：**無可可巧克力（Cocoa-Free Chocolate）**。
對象：越南籍大學生（華語 A2–B1）。含 4 個小遊戲與課堂即時投票。

## 使用方式
- 直接打開網頁即可（手機 / 平板 / 電腦皆可）。
- 老師投影模式：網址加 `?present=1`（不問姓名、不記錄）。
- 學生分班記錄：網址加 `?session=日期_班級`，例如 `?session=20260601_VN1`。

操作：`←` `→` 或畫面下方 `◀ ▶` 換頁、`F` 全螢幕、手機可左右滑動。
